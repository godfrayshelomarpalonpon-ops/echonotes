#!/usr/bin/env python
import sys

# This is a dummy manage.py to satisfy Vercel's automatic Django detection.
# It doesn't need to do anything because we've already handled static files in the /public folder.
if __name__ == "__main__":
    print("Vercel automatic check detected. Satisfying build requirements...")
    sys.exit(0)
